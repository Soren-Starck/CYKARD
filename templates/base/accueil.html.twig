{% extends 'base.html.twig' %}

{% block title %}{{ pagetitle }}{% endblock %}

{% block body %}
    <div><p>Bienvenue sur <strong>BLYAT-CARD</strong>!</p></div>
    <div>
        {% if is_user_logged_in() %}
            <span><a class="link" href="{{ path('app_tableaux') }}">Consulter mes tableaux</a></span>
        {% else %}
            <p>
                Pour créer des tableaux, commencez par vous
                <a class="link" href="{{ path('app_login') }}">connecter</a>
                ou par <a class="link" href="{{ path('app_register') }}">créer un compte</a>!
            </p>
        {% endif %}
    </div>
{% endblock %}